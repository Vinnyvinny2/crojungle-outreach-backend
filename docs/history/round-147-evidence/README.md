# Round 147 — the evidence, as runnable files

Each file lifts REAL declarations out of `src/all.js` by name (`lift.js`) and executes them. Nothing
here reads the code and reasons about it: this repo's record is that reading has been wrong about
causes repeatedly and executing has not ([§102](../round-102.md), [§104](../round-104.md)).

Run any of them with `node docs/history/round-147-evidence/<file>`.

| file | what it proves |
|---|---|
| `floor-decides-the-channel.js` | Trussler's exact live inputs return `channel "call"` with the reason on the row, while a VERIFIED headcount at $12M still leaves the phone, Rose Paving ($255M from a directory figure) stays off it, and a TheirStack lead keeps email-only. It also prints the sharper cause: every medical trade converts at HVAC's $200k per head, and 53 × $200k = $10.6M crossed the $10M channel line by 3%. |
| `a-count-under-three.js` | `"two doctors"` measures nothing in BOTH ladders, `"three doctors"` still reads entry/low, and a two-name read falls through so a practice publishing four locations or a twelve-name team page is still sized. |
| `award-is-not-a-headcount.js` | The raw regex still matches `"50 Doctors"` in "named one of Houston's Top 50 Doctors"; through the real reader, 12 of 12 award phrasings measure nothing and 8 of 8 real counts survive. |

## What is NOT in here, and is named as such

- **Whether the 53 on Trussler's team page is a parse artifact or a real roster.** `drtrussler.com`
  is blocked by this container's egress proxy, so the page could not be fetched and the count could
  not be reproduced from the real HTML. The fix does not depend on the answer; the parser is
  unexamined either way, and so is the 27 at Richard J Garcia CPA.
- **Whether Reoon's `error` is a wrong key or a spent quota.** `reoon.com` is unreachable from this
  network too ([§133](../round-133.md)). The fix makes the vendor's own `reason` reach the log so the
  next batch answers it.
- **How many of the next press's sized leads land large or over the ICP.** Only a real press scores
  that; the press line now reports it.
